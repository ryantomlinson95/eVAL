<!-- Form for rendering groups -->

<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
<%= f.hidden_field :course_id, :value => Course.find(params[:course_id]).id  %>
  </div>
  
  <div class="actions">
    <%= f.submit "Create Group", :class => "btn btn-default" %>
    <%= link_to 'Back', groups_path, :class => "btn btn-default" %>
  </div>
<% end %>
